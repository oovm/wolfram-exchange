{
    "name": "notedown-workspace",
    "private": true,
    "scripts": {
        "cli": "cargo run --color=always --package wxf-converter --bin wxf-converter -- -h",
        "u": "cargo upgrade --offline --incompatible",
        "test": "cargo test --release",
        "fmt": "cargo fmt --all",
        "push": "yarn test && git push",
        "reset": "git reset Head~ --soft"
    }
}
